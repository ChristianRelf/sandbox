import Link from "next/link";import { ArrowRight,LockKeyhole,ShieldCheck } from "lucide-react";
const controls=[{title:"Local execution boundary",body:"Workflow data can remain on the runner. Hosted execution is optional."},{title:"Credential storage",body:"Desktop credentials use the operating-system vault and protected values are redacted from logs."},{title:"Plugin sandbox",body:"WebAssembly plugins receive only declared and approved host capabilities."},{title:"Runner identity",body:"Paired runners authenticate signed requests with rotatable device keys."},{title:"Hosted isolation",body:"Hosted runner and browser-worker designs define workload and profile isolation boundaries."},{title:"Auditability",body:"Workspace governance and runner operations are recorded through control-plane audit events."}];
export const metadata={title:"Security",description:"sndbox security architecture, implemented controls and current assurance status."};export default function Page(){return <main id="content" className="index-page security-page"><header><p className="eyebrow"><span/>Security</p><h1>Control starts with<br/>where the work runs.</h1><p>sndbox limits access at the file, command, network, credential, plugin and runner boundaries.</p><Link className="sb-button sb-button--primary" href="/contact?type=security-documents">Request security documents <ArrowRight size={14}/></Link></header><section className="assurance"><ShieldCheck size={21}/><div><strong>Assurance status</strong><p>Controls documented and internally tested. No independent certification evidence is present in this checkout; sndbox does not claim certification.</p></div></section><section className="control-grid">{controls.map(item=><article key={item.title}><LockKeyhole size={16}/><h2>{item.title}</h2><p>{item.body}</p></article>)}</section></main>}
