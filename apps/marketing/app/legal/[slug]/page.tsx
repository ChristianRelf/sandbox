import { notFound } from "next/navigation";import { legalPages } from "@sandbox/content";type Params=Promise<{slug:string}>;export function generateStaticParams(){return legalPages.map(slug=>({slug}))}export default async function Page({params}:{params:Params}){const {slug}=await params;if(!legalPages.includes(slug as never))notFound();const title=slug.split("-").map(v=>v[0].toUpperCase()+v.slice(1)).join(" ");return <main id="content" className="legal-page"><p className="eyebrow"><span/>Legal</p><h1>{title}</h1><aside><strong>Professional review required before publication.</strong><p>This repository does not contain approved legal text for this policy. The route is intentionally excluded from substantive publication rather than presenting generated terms that may contradict product behaviour.</p></aside><h2>Review inputs</h2><ul><li>Local and hosted execution data flows</li><li>Account, marketplace and billing processors</li><li>Retention, deletion and support-access behaviour</li><li>Regional availability and consumer rights</li></ul></main>}
