---
title: "Set Data"
description: "Construct a structured object. Exact configuration, ports, placement, and execution behavior."
icon: "braces"
---

**Node type:** `set_data` · **Version:** `1` · **Category:** Data

Construct a structured object. This node is not marked with the catalogue's generic side-effect flag. That classification is not a guarantee that every configured operation is read-only; review the concrete action and its destination.

## When to use this node

- Give several upstream values a stable object shape before passing them on.
- Create a small request body or notification payload without writing code.

## Example: Build a normalized incident object

Combine mapped values into one object that later nodes can consume consistently.


```json title="Example settings"
{
  "values": {
    "service": "checkout",
    "status": "{{nodes.request.output.status}}",
    "urgent": true
  }
}
```

This shows the important settings, not a complete exported node.


1. Add the desired keys in values.
2. Map changing fields from earlier outputs.
3. Map the value output into an HTTP, AI, or Code node.


## Configuration

| Field | Type | Default | What it controls |
| --- | --- | --- | --- |
| `values` | object | `{"key":"value"}` | Structured JSON object produced by the node. |



## Workflow JSON

The editor stores this node with the following implemented default configuration:

```json title="Default node configuration"
{
  "type": "set_data",
  "version": 1,
  "configuration": {
    "values": {
      "key": "value"
    }
  }
}
```

Values may be entered literally or mapped from an earlier compatible output when the inspector exposes a mapping control. See [Variables and data mapping](/workflows/variables-and-data).

## Inputs

| Port | Type | Required | Description |
| --- | --- | --- | --- |
| `values` | `object` | No | Object |

## Outputs

| Port | Type | Required | Description |
| --- | --- | --- | --- |
| `value` | `object` | No | Object |

## Execution behavior

- The output is the resolved object; nested JSON-compatible values are preserved.
- Set Data transforms execution data only and does not persist workflow state between runs.
- The editor catalogue declares `value` (object) for mapping. The execution inspector can contain additional evidence fields; inspect a real result before selecting nested paths from object or any outputs.

## Common issues

- Use Set Workflow State if the value must survive the current execution.
- Do not put secrets in a literal object; use protected values or connections at the consuming node.


## Where it can run

- Desktop local runner
- Paired self-hosted runner
- Hosted runner

## Test and inspect

Use **Test node** in the editor to preview this step with the current configuration. A full run records resolved inputs, outputs, logs, duration, and any artifacts in the execution inspector. Side-effecting or destructive nodes can require an additional confirmation or approved workflow permission.

## Related guides

- [Variables and data mapping](/workflows/variables-and-data)
- [JSON data](/files-and-data/json)
