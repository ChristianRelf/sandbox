import type{MetadataRoute}from"next";import{docs}from"../lib/content";export default function sitemap():MetadataRoute.Sitemap{const generated=["developers/api/reference","nodes/reference"].map(slug=>({url:`https://docs.sandbox.com/${slug}`,lastModified:new Date("2026-08-28")}));return [...docs.map(p=>({url:`https://docs.sandbox.com/${p.slug}`,lastModified:new Date(p.reviewed)})),...generated]}
