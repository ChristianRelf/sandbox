import type{MetadataRoute}from"next";import{brand}from"@sandbox/brand";import{docs}from"../lib/content";export default function sitemap():MetadataRoute.Sitemap{const generated=["developers/api/reference","nodes/reference"].map(slug=>({url:`${brand.domains.docs}/${slug}`,lastModified:new Date("2026-08-28")}));return [...docs.map(p=>({url:`${brand.domains.docs}/${p.slug}`,lastModified:new Date(p.reviewed)})),...generated]}
