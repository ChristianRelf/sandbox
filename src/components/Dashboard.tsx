import * as DropdownMenu from "@radix-ui/react-dropdown-menu";
import { Clock3, MoreHorizontal, Play, Plus, Search, Trash2 } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { formatDistanceToNow } from "date-fns";
import { api } from "../api";
import { definitionFor } from "../catalogue";
import { useAppStore } from "../store";
import { Status } from "./Status";
import { EmptyState } from "./EmptyState";
export function Dashboard(){const {workflows,load,loading,openWorkflow,createWorkflow,deleteWorkflow}=useAppStore();const [search,setSearch]=useState("");const [running,setRunning]=useState<string>();useEffect(()=>{load()},[load]);const filtered=useMemo(()=>workflows.filter(w=>w.workflow.name.toLowerCase().includes(search.toLowerCase())),[workflows,search]);const run=async(id:string)=>{setRunning(id);try{await api.runWorkflow(id);await load()}finally{setRunning(undefined)}};if(!loading&&!workflows.length)return <main className="content"><PageHeader onCreate={()=>createWorkflow()}/><EmptyState onCreate={()=>createWorkflow()} onTemplate={createWorkflow}/></main>;return <main className="content"><PageHeader onCreate={()=>createWorkflow()}/><div className="toolbar"><div className="search"><Search size={15}/><input aria-label="Search workflows" placeholder="Search workflows…" value={search} onChange={e=>setSearch(e.target.value)}/><kbd>/</kbd></div><span className="count">{filtered.length} workflow{filtered.length===1?"":"s"}</span></div><section className="workflow-table" aria-label="Workflows"><div className="table-head"><span>Name</span><span>Trigger</span><span>Last run</span><span>Next run</span><span/></div>{filtered.map(({workflow,lastExecution,nextRunAt})=>{const trigger=workflow.nodes.find(n=>n.id===workflow.triggerNodeId);const icon=trigger?definitionFor(trigger.type).icon:Clock3;const TriggerIcon=icon;return <div className="workflow-row" key={workflow.id} tabIndex={0} onDoubleClick={()=>openWorkflow(workflow.id)} onKeyDown={e=>e.key==="Enter"&&openWorkflow(workflow.id)}><div className="workflow-name"><span className={`enable-dot ${workflow.enabled?"enabled":""}`}/><div><b>{workflow.name}</b><small>{workflow.description||`${workflow.nodes.length} nodes · Updated ${formatDistanceToNow(new Date(workflow.updatedAt),{addSuffix:true})}`}</small></div></div><div className="muted-cell"><TriggerIcon size={14}/>{trigger?.name??"Missing trigger"}</div><div>{lastExecution?<><Status status={lastExecution.status}/><small>{formatDistanceToNow(new Date(lastExecution.startedAt),{addSuffix:true})}</small></>:<span className="muted">Never run</span>}</div><div className="muted-cell">{nextRunAt?<><Clock3 size={14}/>{formatDistanceToNow(new Date(nextRunAt),{addSuffix:true})}</>:<span>—</span>}</div><div className="row-actions"><button className="icon-button" title="Run workflow" disabled={running===workflow.id} onClick={()=>run(workflow.id)}><Play size={14} fill="currentColor"/></button><DropdownMenu.Root><DropdownMenu.Trigger asChild><button className="icon-button"><MoreHorizontal size={16}/></button></DropdownMenu.Trigger><DropdownMenu.Portal><DropdownMenu.Content className="menu" align="end"><DropdownMenu.Item onSelect={()=>openWorkflow(workflow.id)}>Open workflow</DropdownMenu.Item><DropdownMenu.Item onSelect={()=>run(workflow.id)}>Run now</DropdownMenu.Item><DropdownMenu.Separator/><DropdownMenu.Item className="danger" onSelect={()=>confirm(`Delete ${workflow.name}?`)&&deleteWorkflow(workflow.id)}><Trash2 size={14}/>Delete</DropdownMenu.Item></DropdownMenu.Content></DropdownMenu.Portal></DropdownMenu.Root></div></div>})}</section>{workflows.length>0&&<section className="compact-templates"><h3>Start from a template</h3><button onClick={()=>createWorkflow("website-health")}><b>Website Health Monitor</b><small>Manual → HTTP → Condition → Notification</small></button><button onClick={()=>createWorkflow("downloads-organiser")}><b>Downloads Folder Organiser</b><small>File watch → Condition → Move file</small></button></section>}</main>}
function PageHeader({onCreate}:{onCreate:()=>void}){return <header className="page-header"><div><h1>Workflows</h1><p>Build and run automations on this device.</p></div><button className="button primary" onClick={onCreate}><Plus size={15}/>Create workflow</button></header>}
