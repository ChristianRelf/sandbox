import { Command, Search } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { NODE_DEFINITIONS } from "../catalogue";
import type { NodeType } from "../types";
export function CommandPalette({open,onClose,onAdd,onCreate}:{open:boolean;onClose:()=>void;onAdd?:(type:NodeType)=>void;onCreate:()=>void}){const [query,setQuery]=useState("");useEffect(()=>{if(open)setQuery("")},[open]);const nodes=useMemo(()=>NODE_DEFINITIONS.filter(n=>(n.name+n.description+n.group).toLowerCase().includes(query.toLowerCase())),[query]);if(!open)return null;return <div className="overlay" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="command-modal" role="dialog" aria-modal="true"><div className="command-search"><Search size={17}/><input autoFocus placeholder="Search commands and nodes…" value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==="Escape"&&onClose()}/><kbd>ESC</kbd></div><div className="command-results"><div className="command-group"><label>Actions</label><button onClick={()=>{onCreate();onClose()}}><span className="command-icon"><Command size={15}/></span><span><b>Create workflow</b><small>Start with a manual trigger</small></span></button></div>{onAdd&&<div className="command-group"><label>Nodes</label>{nodes.map(node=><button key={node.type} onClick={()=>{onAdd(node.type);onClose()}}><span className="command-icon"><node.icon size={15}/></span><span><b>{node.name}</b><small>{node.description}</small></span><em>{node.group}</em></button>)}</div>}</div></div></div>}
