import { ArrowRight, GitFork, Plus } from "lucide-react";
export function EmptyState({onCreate,onTemplate}:{onCreate:()=>void;onTemplate:(key:string)=>void}){return <div className="empty-wrap"><div className="empty-icon"><GitFork size={22}/></div><h2>Your automations start here</h2><p>Connect a trigger to an action and let it run locally.</p><button className="button primary" onClick={onCreate}><Plus size={15}/>Create workflow</button><div className="starter-label">Or start from a working template</div><div className="templates"><button onClick={()=>onTemplate("website-health")}><div><span className="template-icon">↗</span><b>Website Health Monitor</b><small>Request a URL, check the status, notify.</small></div><ArrowRight size={16}/></button><button onClick={()=>onTemplate("downloads-organiser")}><div><span className="template-icon">⌁</span><b>Downloads Folder Organiser</b><small>Watch for files and move matches safely.</small></div><ArrowRight size={16}/></button></div></div>}
