# Request Approval

Creates an external approval using a host-owned credential operation. The WebAssembly guest receives neither the provider token nor the local vault identifier. Execution requires an idempotency key. Node v1 migrates explicitly to v2 and existing workflows remain pinned until that migration is approved.
