[package]
name = "sandbox-example-weather-guest"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
base64 = "0.23"
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true

[workspace]
